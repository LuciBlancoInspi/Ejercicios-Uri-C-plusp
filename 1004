
#include<iostream>
using namespace std;

int main(){
    int A, B, PROD;
    cin >> A >> B ;                        //scanf("%d%d", &A,&B);
    PROD=A*B;
    cout << "SOMA = " << PROD<< endl;    // printf("SOMA = %d\n", A+B);
    return 0;
}
